// Package buffer will hold the host agent's disk-backed event buffer, letting the
// agent ride out platform outages without dropping telemetry. Not implemented in
// this slice — see CLAUDE.md for the phasing.
package buffer
