# --- Secrets / env -----------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key
*.crt

# --- Local-only infra reference: real hostnames/IPs/topology, never public -
docs/INFRASTRUCTURE.md

# --- Go (go-agent-v0, archived) -------------------------------------------
/go-agent-v0/bin/
*.exe
*.test
*.out

# --- Python (graph, threat-intel, evaluator, attacker, defender) ---------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# --- Node / frontend -------------------------------------------------------
node_modules/
.next/
out/
dist/
build/

# --- Neo4j local data ----------------------------------------------------
/graph/data/
/graph/import/

# --- Lab artifacts (don't commit captured pcaps, dumped creds, etc.) -----
/lab/captures/
/lab/dumps/

# --- OS / IDE --------------------------------------------------------------
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# --- Claude Code: personal/local-only memory, never shared via git -------
CLAUDE.local.md
.claude/settings.local.json

# --- Logs ------------------------------------------------------------------
*.log
